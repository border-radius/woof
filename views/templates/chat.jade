.chat
  message(ng-repeat="message in show.messages|orderBy:'-date'" content="message")
  message(ng-if="show.message" content="show.message")
  message(ng-repeat="reply in show.replies|orderBy:'date'" content="reply")